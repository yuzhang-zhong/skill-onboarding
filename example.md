# 样例

两种形态各一张。**照这个密度写，不要更长。**

---

## 一、普通卡 · 单个 skill

对某"去 AI 味"技能跑完流程后的产出。

```
✅ 部分可用：核心功能实测正常；但该技能处于停用状态，且文档提到的配套步骤未随包安装

专治中文写作的"AI 味"——检测 + 量化分级 + 改写。强在规则库，弱在自动化：多数改动需要人来判断。

🎯 最常用的三个动作

1. 「帮我扫一下这几章的 AI 味」→ 出量化报告：禁用词密度、排比段数、心理词占比，自动给轻/中/重分级并列出命中项
2. 「把这段改得不像 AI 写的」→ 按三遍流程改：去泛化词 → 去书面腔 → 回自然感
3. 「这段读着别扭但我说不出哪不对」→ 走深层描写层，指出问题类型并给大师级改法

💡 你可能不知道还能这样用

- 「按余华的路子改这段」→ 内置六套可以点名指定的手法：视觉代替心理、动作外化情绪、白描胜形容词、环境烘托、用缺席写存在、动作带话
- 「哪些能自动改，哪些得我自己看？」→ 有明确分工：标点连用、高频套话这类能自动修；语境敏感的句式只报告不自动改，避免误伤
- 「怎么让它真正能用起来？」→ 目前是停用状态，需要在技能管理里启用后才会被自动调用

🔑 记不住就存这句

「扫一下这段的 AI 味，重的直接改」
```

**要点**：状态行说了实话（跑了、但也发现两个问题）；💡 第一条是用户凭名字绝对猜不到的东西；最后一条把发现的问题变成可执行动作。

---

## 二、CLI 工具卡 · 一个 CLI 覆盖多个 skill

21 个飞书技能合并成的一张卡。

```
✅ 可用。CLI 本体正常，自检全部通过。两处要留意：内置说明文档比主程序落后一个版本；当前生效的应用身份与配置里指定的那个不一致。

飞书全家桶——一个命令覆盖 26 个业务域。聊天、文档、表格、云盘、日程、任务、审批、考勤、OKR、邮件全在内。**你本地那 21 个飞书技能，底层都是它一个。**

🧭 你想做什么

· 聊消息 / 管群  → 发消息、搜聊天记录、拉群、加群成员、置顶收藏、电话/短信加急
· 写文档        → 建文档、读内容、插图插文件、搜文档、历史版本回滚
· 做表格        → 读写单元格、公式校验、图表、透视表、条件格式、导入导出 Excel/CSV
· 管云盘文件    → 上传下载、复制移动、评论、协作者权限、本地文件夹双向同步
· 排日程 / 开会  → 看日程、查忙闲、找会议室、智能推荐时间、回复邀请、转移组织者
· 记待办        → 建任务、标记完成、提醒、任务清单、搜索、传附件
· 知识库 / 幻灯片 / 画板 / 审批 / 考勤 / OKR / 邮箱 → 都有，需要时直接说

💡 你可能不知道还能这样用

- 「先预览一下，别真发」→ 所有操作都支持预演模式，涉及发消息、改文档时能先看结果再决定
- 「你自查一下飞书这边有没有问题」→ 一条命令做全身体检：版本、授权、身份、网络，出问题直接定位
- 「把这周的日程和待办一起给我」→ 已有现成的组合用法，一句话出当天/本周安排摘要
- 「本地这个文件夹同步到飞书云盘」→ 支持目录级镜像和双向同步，不用一个个手动传
- 「表格里加个透视表和条件格式」→ 表格域比想的深很多：透视表、条件格式、迷你图、历史版本回退都能做

🔑 记不住就存这句

「飞书那边帮我……，先预览一遍」
```

**要点**：

- **第二段换成能力地图**，按"你会说的话"分组，不按命令分组。命令名对用户毫无意义。
- 开头一句话点破关系：21 个技能 = 1 个工具。这是用户最需要知道的。
- 💡 第一条必须是**跨域通用但没人知道**的能力（这里是预演模式），价值最高。
- 💡 最后一条来自**差集**：CLI 帮助里列出的域 vs 本地已有的技能，差出来的就是用户完全不知道的。
- 全程没有出现任何路径、文件名、仓库来源。
