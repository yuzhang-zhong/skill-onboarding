# 样例

两种形态各一张。照这个密度写，不要更长。

---

## 一、单个 skill

对某"去 AI 味"技能跑完流程后的产出。

```
⚠️ 部分可用。核心功能实测正常，但该技能处于停用状态，文档提到的配套步骤也没随包装上。

中文写作的去 AI 味工具。检测、分级、改写都有。强项是规则库，弱项是自动化，多数改动得人来判断。

能干什么

    扫一遍找 AI 味     出量化报告，禁用词密度、排比段数、心理词占比，顺带给轻中重分级
    改得像人写的       按三遍流程走，去泛化词，去书面腔，回自然感
    我说不出哪别扭     走深层描写层，指出是情绪宣告还是心理直陈，给大师级改法

几个多半不知道的

    按余华的路子改这段   内置六套可以点名的笔法，视觉代替心理、动作外化情绪、白描胜形容词、环境烘托、用缺席写存在、动作带话
    哪些能自动改         标点连用、高频套话能自动修，语境敏感的句式只报告不动手，免得误伤
    怎么才能用起来       停用状态，得去技能管理里启用之后才会自动触发

记不住就存这句

    扫一下这段的 AI 味，重的直接改
```

**要点**：状态行说了实话，跑了，也发现两个问题。最后一条把发现的问题变成了下一步动作。中间那段的三个口令谁都能直接说出口。

---

## 二、一组 skill

21 个飞书技能合并成的一张卡。

```
✅ 可用。CLI 本体没问题，自检全过。两处要留意，内置说明文档比主程序落后一个版本，当前生效的应用身份和配置里指定的那个也对不上。

飞书全家桶。26 个业务域，你本地那 21 个飞书技能，底层都是它一个。

能干什么

    聊消息、管群     发消息、搜记录、拉群、加成员、置顶、电话短信加急
    写文档          建文档、读内容、插图插文件、搜文档、回滚版本
    做表格          读写单元格、公式校验、图表、透视表、条件格式、导入导出
    管云盘          上传下载、复制移动、评论、协作权限、文件夹双向同步
    排日程、开会     看日程、查忙闲、找会议室、推荐时间、回复邀请
    记待办          建任务、标完成、提醒、清单、搜索、传附件

几个多半不知道的

    先预览，别真发       所有操作都能先跑一遍看结果，发消息改文档时最有用
    自查一下飞书         一条命令查版本、授权、身份、网络
    日程和待办一起给我    现成的组合用法，一句话出当天或本周安排
    整个文件夹同步上去    目录级镜像和双向同步，不用一个个传
    思维笔记、斜杠命令    底层支持，本地没对应技能，直接说就能用

记不住就存这句

    飞书那边帮我……，先预览一遍
```

**要点**：

- 开头一句点破关系。21 个技能等于 1 个工具，这是用户最需要先知道的事。
- "能干什么"按用户会说的话分组，不按命令名分组。命令名对用户没有意义。
- "几个多半不知道的"第一条必须是跨域通用、但没人知道的能力，比如这里的预演模式。
- 最后一条来自差集。工具支持的减掉本地已装的，差出来的就是用户不知道的。
- 全程没有出现路径、文件名、来源。
